% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surv_xl_grf_lasso.R
\name{surv_xl_grf_lasso}
\alias{surv_xl_grf_lasso}
\title{X-learner of lasso and grf}
\usage{
surv_xl_grf_lasso(X, W, Y, D, times, alpha = 0.05, ps = NULL, cen_fit = "KM")
}
\arguments{
\item{X}{The baseline covariates}

\item{W}{The treatment variable (0 or 1)}

\item{Y}{The follow-up time}

\item{D}{The event indicator}

\item{times}{The prediction time of interest}

\item{alpha}{Imbalance tuning parameter for a split (see grf documentation)}

\item{ps}{The propensity score}

\item{cen_fit}{The choice of model fitting for censoring}
}
\value{
A vector of estimated conditional average treatment effects
}
\description{
X-learner, implemented via grf (for nuisance parameter estimation)
and glmnet (lasso) with 'coxph' distribution (for target function CATE estimation)
}
\examples{
\donttest{
n = 1000; p = 25
times = 0.2
Y.max <- 2
X <- matrix(rnorm(n * p), n, p)
W <- rbinom(n, 1, 0.5)
numeratorT <- -log(runif(n))
T <- (numeratorT / exp(1 * X[,1] + (-0.5 - 1 * X[,2]) * W))^2
failure.time <- pmin(T, Y.max)
numeratorC <- -log(runif(n))
censor.time <- (numeratorC/(4^2))^(1/2)
Y <- pmin(failure.time, censor.time)
D <- as.integer(failure.time <= censor.time)
n.test <- 500
X.test <- matrix(rnorm(n.test * p), n.test, p)

surv_xl_grf_lasso_fit = surv_xl_grf_lasso(X, W, Y, D, times, ps = 0.5)
cate = predict(surv_xl_grf_lasso_fit)
cate.test = predict(surv_xl_grf_lasso_fit, X.test)
}
}
